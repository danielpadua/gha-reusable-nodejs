version: 2
updates:
  # Keep the GitHub Actions referenced by the reusable workflow up to date.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
